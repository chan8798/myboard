<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title><%= user.userid %>님의 친구 리스트</title>
  <style>
      body {
      font-family: Arial, sans-serif;
      margin: 20px;
    }
    .top-bar {
      display: flex;
      justify-content: flex-start;
      margin-bottom: 20px;
    }
    .home-btn {
      padding: 8px 15px;
      font-size: 1rem;
      background-color: #ddadd1;
      border: none;
      color: white;
      border-radius: 5px;
      cursor: pointer;
      text-decoration: none;
    }
    .home-btn:hover {
      background-color: #d27fa6;
    }
    form {
      margin-bottom: 20px;
    }
    input[type="text"] {
      padding: 8px;
      font-size: 1rem;
      width: 300px;
    }
    button {
      padding: 8px 15px;
      font-size: 1rem;
      background-color: #ddadd1;
      border: none;
      color: white;
      border-radius: 5px;
      cursor: pointer;
    }
    button:hover {
      background-color: #d27fa6;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 15px;
    }
    th, td {
      border: 1px solid #ccc;
      padding: 10px 15px;
      text-align: left;
    }
    th {
      background-color: #fabdf3;
    }
    a.blog-link {
      color: #d27fa6;
      text-decoration: none;
    }
    a.blog-link:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="top-bar">
    <a class="home-btn" href="/blog/<%= user.userid %>">블로그</a>
  </div>
  <h2><%= user.userid %>님의 친구 리스트</h2>
  
  <% if (friends.length === 0) { %>
    <p>친구가 없습니다.</p>
  <% } else { %>
    <table>
      <thead>
        <tr>
          <th>이름</th>
          <th>아이디</th>
          <th>블로그 보기</th>
          <th>삭제</th>
        </tr>
      </thead>
      <tbody>
        <% friends.forEach(friend => { %>
          <tr>
            <td><%= friend.name %></td>
            <td><%= friend.userid %></td>
            <td><a href="/friendblog/<%= friend.userid %>">블로그 보기</a></td>
            <td>
              <form action="/deletefriend" method="POST" style="display:inline;">
                <input type="hidden" name="friendId" value="<%= friend.userid %>">
                <button type="submit">삭제</button>
              </form>
            </td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  <% } %>
</body>
</html>
