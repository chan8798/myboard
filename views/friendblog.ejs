<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title><%= friend.userid %>(<%= friend.name %>)님의 블로그</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
    }
    .navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background-color: #ddadd1;
      padding: 10px 20px;
      color: white;
    }
    .navbar .logo {
      font-size: 1.5em;
      font-weight: bold;
    }
    .navbar .nav-buttons button,
    .navbar .nav-buttons a {
      background-color: #ddadd1;
      color: white;
      border: none;
      padding: 8px 15px;
      margin-left: 10px;
      border-radius: 5px;
      cursor: pointer;
      text-decoration: none;
    }
    .navbar .nav-buttons button:hover,
    .navbar .nav-buttons a:hover {
      background-color: #d27fa6;
    }
  </style>
</head>
<body>
  <nav class="navbar">
    <div class="logo"><%= friend.name %> / <%= friend.userid %>  님의 블로그</div>
    <div class="nav-buttons">
       <a href="/friend/<%= friend.userid %>">친구</a>
       <a href="/home/<%=user.userid%>">홈</a>
    </div>

  </a>
  </nav>
  <h2> <%= friend.userid %>의 게시물</h2>
  <% if (posts.length === 0) { %>
  <p>작성한 게시물이 없습니다.</p>
<% } else { %>
  <% posts.forEach(post => { %>
    <div style="border:1px solid #ccc; padding:15px; margin-bottom:20px; border-radius:8px;">
      <h3><%= post.title %></h3>
      <p><%= post.content %></p>
      <small>작성일: <%= new Date(post.createdAt).toLocaleString() %></small>

      <% if (post.imagePath) { %>
        <div style="margin-top:10px;">
          <img src="<%= post.imagePath %>" alt="게시물 이미지" style="max-width:300px; border-radius:10px;">
        </div>
      <% } %>
    </div>
      </div>
  <% }); %>
<% } %>

</body>
</html>
